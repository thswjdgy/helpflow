// HelpFlow 기본 위젯 테스트
// TODO: 각 화면별 위젯 테스트는 기능 구현 후 추가 예정

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('앱 기본 smoke test', (WidgetTester tester) async {
    // 현재는 Hive 초기화가 필요하여 통합 테스트로 대체 예정
    // 2주차 이후 각 화면 위젯 테스트 추가
    expect(true, isTrue);
  });
}
